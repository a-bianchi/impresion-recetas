// @flow
import React, { Component } from 'react';
import Receta from '../components/Receta';

type Props = {};

export default class RecetaPage extends Component<Props> {
  props: Props;

  render() {
    return <Receta />;
  }
}
